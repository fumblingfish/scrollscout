{
  "name": "scrollscout_gh-pages",
  "version": "0.0.1",
  "devDependencies": {
    "react-scripts": "0.4.0"
  },
  "homepage": "./",
  "dependencies": {
    "classnames": "^2.2.5",
    "rc-slider": "^5.1.1",
    "react": "^15.3.1",
    "react-dom": "^15.3.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && npm run deploy:git && npm run deploy:subtree",
    "deploy:git": "git add build --all && git commit -m \"Release $(date)\" && git push",
    "deploy:subtree": "git subtree push --prefix build origin gh-pages"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
